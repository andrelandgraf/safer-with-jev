# Neon-managed. `neon env pull` / `neon checkout` write these to .env.local.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
NEON_AI_GATEWAY_TOKEN=
NEON_AI_GATEWAY_BASE_URL=
NEON_FUNCTION_GATEWAY_BASE_URL=
NEON_BRANCH=

# Function secrets. Copy into .env.local; `neon deploy --env .env.local` uploads them.
TYPESAFE_API_KEY=
